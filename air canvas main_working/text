import fitz
# PDF document initialization
pdf_doc = fitz.open()
pdf_page = pdf_doc.new_page(width=1280, height=720)

# Save the canvas as a PDF document if 's' is pressed
    if k == ord("s"):
        # Create a new page in the PDF document
        pdf_page = pdf_doc.new_page(width=1280, height=720)
        pix = fitz.Pixmap(
            fitz.csRGB, (canvas.shape[1], canvas.shape[0]), canvas.tobytes()
        )
        pdf_page.insert_pixmap(0, 0, pix)

# Save the PDF document
pdf_doc.save("annotated_canvas.pdf")